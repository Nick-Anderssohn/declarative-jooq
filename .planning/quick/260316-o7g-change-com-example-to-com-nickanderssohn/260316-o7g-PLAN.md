---
phase: quick
plan: 260316-o7g
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle-plugin/build.gradle.kts
  - "dsl-runtime/src/*/kotlin/com/nickanderssohn/declarativejooq/**/*.kt"
  - "codegen/src/*/kotlin/com/nickanderssohn/declarativejooq/**/*.kt"
  - "gradle-plugin/src/*/kotlin/com/nickanderssohn/declarativejooq/**/*.kt"
  - "integration-tests/src/test/kotlin/com/nickanderssohn/declarativejooq/**/*.kt"
autonomous: true
requirements: []
must_haves:
  truths:
    - "All source files use com.nickanderssohn package prefix instead of com.example"
    - "Gradle plugin ID is com.nickanderssohn.declarative-jooq"
    - "All tests pass after rename"
  artifacts:
    - path: "dsl-runtime/src/main/kotlin/com/nickanderssohn/declarativejooq/"
      provides: "Runtime source files under new package"
    - path: "codegen/src/main/kotlin/com/nickanderssohn/declarativejooq/"
      provides: "Codegen source files under new package"
    - path: "gradle-plugin/src/main/kotlin/com/nickanderssohn/declarativejooq/"
      provides: "Plugin source files under new package"
  key_links:
    - from: "codegen emitters (BuilderEmitter, DslScopeEmitter, DslResultEmitter)"
      to: "dsl-runtime types"
      via: "ClassName string literals"
      pattern: 'ClassName\("com\.nickanderssohn\.declarativejooq"'
    - from: "gradle-plugin/build.gradle.kts"
      to: "DeclarativeJooqPlugin class"
      via: "implementationClass string"
      pattern: "com\\.nickanderssohn\\.declarativejooq\\.gradle\\.DeclarativeJooqPlugin"
---

<objective>
Rename the Java/Kotlin package prefix from `com.example` to `com.nickanderssohn` throughout the entire repository.

Purpose: Replace placeholder package name with the real owner namespace before publishing.
Output: All source files, build config, and directory structure use `com.nickanderssohn` prefix. All tests pass.
</objective>

<execution_context>
@/Users/nick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@build.gradle.kts
@gradle-plugin/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move directory trees and update all package references</name>
  <files>
    All 31 .kt files under src/*/kotlin/com/example/ across dsl-runtime, codegen, gradle-plugin, integration-tests
    gradle-plugin/build.gradle.kts
    build.gradle.kts (add group if needed)
  </files>
  <action>
    Rename the package from com.example to com.nickanderssohn in all source code and build configuration. This is a mechanical find-and-replace plus directory move. Steps:

    1. For each of the 7 source directories (listed below), use `git mv` to move `com/example` to `com/nickanderssohn`:
       - dsl-runtime/src/main/kotlin/com/example -> dsl-runtime/src/main/kotlin/com/nickanderssohn
       - dsl-runtime/src/test/kotlin/com/example -> dsl-runtime/src/test/kotlin/com/nickanderssohn
       - codegen/src/main/kotlin/com/example -> codegen/src/main/kotlin/com/nickanderssohn
       - codegen/src/test/kotlin/com/example -> codegen/src/test/kotlin/com/nickanderssohn
       - gradle-plugin/src/main/kotlin/com/example -> gradle-plugin/src/main/kotlin/com/nickanderssohn
       - gradle-plugin/src/test/kotlin/com/example -> gradle-plugin/src/test/kotlin/com/nickanderssohn
       - integration-tests/src/test/kotlin/com/example -> integration-tests/src/test/kotlin/com/nickanderssohn

    2. In ALL .kt source files (the 31 files that were moved), replace every occurrence of the string `com.example` with `com.nickanderssohn`. This covers:
       - `package com.example.declarativejooq` -> `package com.nickanderssohn.declarativejooq`
       - `import com.example.declarativejooq` -> `import com.nickanderssohn.declarativejooq`
       - `ClassName("com.example.declarativejooq"` -> `ClassName("com.nickanderssohn.declarativejooq"`
       - String literals like `"com.example.declarativejooq"` -> `"com.nickanderssohn.declarativejooq"`
       - String literals like `"com.example.generated"` -> `"com.nickanderssohn.generated"`

    3. In gradle-plugin/build.gradle.kts:
       - Change `id = "com.example.declarative-jooq"` to `id = "com.nickanderssohn.declarative-jooq"`
       - Change `implementationClass = "com.example.declarativejooq.gradle.DeclarativeJooqPlugin"` to `implementationClass = "com.nickanderssohn.declarativejooq.gradle.DeclarativeJooqPlugin"`

    4. In root build.gradle.kts, add `group = "com.nickanderssohn"` in the subprojects block if not already present.

    5. Verify no remaining references: run `grep -r "com\.example" --include="*.kt" --include="*.gradle.kts"` across the repo (excluding build/ and .planning/ dirs). Must return zero results.

    IMPORTANT: Do NOT update .planning/ directory files -- those are historical documentation. Only update source code and build configuration.
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && grep -r "com\.example" --include="*.kt" --include="*.gradle.kts" --include="*.properties" . | grep -v "build/" | grep -v ".planning/" | grep -v ".gradle/" | wc -l | tr -d ' '</automated>
    Expected output: 0
  </verify>
  <done>All source files and build config reference com.nickanderssohn instead of com.example. No com/example directories remain under src/. No .kt or .gradle.kts files outside .planning/ reference com.example.</done>
</task>

<task type="auto">
  <name>Task 2: Clean build artifacts and verify all tests pass</name>
  <files>No new files -- validation only</files>
  <action>
    1. Delete all build/ directories to ensure no stale compiled classes with old package names remain:
       `./gradlew clean`

    2. Run the full test suite:
       `./gradlew test`

    3. If any test fails due to the rename, diagnose and fix. Common issues:
       - Hardcoded class name strings in test assertions (e.g., `"com.example.declarativejooq.OrganizationTable"` in ScannerTest.kt)
       - Generated code referencing old package in string templates
       - Plugin functional test with hardcoded plugin ID

    All of these should have been caught by the sed replacement in Task 1, but verify.
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && ./gradlew clean test 2>&1 | tail -5</automated>
    Expected: BUILD SUCCESSFUL
  </verify>
  <done>./gradlew clean test passes. All modules compile and all tests green with the new com.nickanderssohn package prefix.</done>
</task>

</tasks>

<verification>
1. `grep -r "com\.example" --include="*.kt" --include="*.gradle.kts" . | grep -v build/ | grep -v .planning/ | grep -v .gradle/` returns nothing
2. No `com/example` directories exist under any `src/` tree
3. `./gradlew clean test` passes across all modules
</verification>

<success_criteria>
- Zero references to com.example in source code and build files
- All directory trees use com/nickanderssohn instead of com/example
- Gradle plugin ID is com.nickanderssohn.declarative-jooq
- Full test suite passes (./gradlew test)
</success_criteria>

<output>
After completion, create `.planning/quick/260316-o7g-change-com-example-to-com-nickanderssohn/260316-o7g-SUMMARY.md`
</output>
