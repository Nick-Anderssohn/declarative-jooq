---
phase: 05-child-table-named-builder-functions
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - codegen/src/test/kotlin/com/nickanderssohn/declarativejooq/codegen/CodeGeneratorTest.kt
  - integration-tests/src/test/kotlin/com/nickanderssohn/declarativejooq/integration/FullPipelineTest.kt
autonomous: true
requirements: [NAME-01, NAME-04]

must_haves:
  truths:
    - "Generated DSL code compiles and executes with new builder function names"
    - "Test harnesses use appUser { } (not organization { }) for app_user builders nested under organization"
    - "Test harnesses use category { } (not childCategory { }) for self-referential category builders"
    - "All existing compile-and-run tests pass with updated builder names"
    - "Full pipeline integration tests pass against Postgres with updated builder names"
  artifacts:
    - path: "codegen/src/test/kotlin/com/nickanderssohn/declarativejooq/codegen/CodeGeneratorTest.kt"
      provides: "Updated test harness strings + compile-and-run tests"
      contains: "appUser {"
    - path: "integration-tests/src/test/kotlin/com/nickanderssohn/declarativejooq/integration/FullPipelineTest.kt"
      provides: "Updated integration harness strings"
      contains: "appUser {"
  key_links:
    - from: "CodeGeneratorTest.kt testHarnessSource()"
      to: "Generated OrganizationBuilder"
      via: "KotlinCompilation classloader"
      pattern: "appUser"
    - from: "FullPipelineTest.kt integrationHarnessSource()"
      to: "Generated OrganizationBuilder"
      via: "KotlinCompilation classloader"
      pattern: "appUser"
---

<objective>
Update all test harness strings in CodeGeneratorTest and FullPipelineTest to use the new child-table-named builder functions, then verify all compile-and-run tests pass.

Purpose: The MetadataExtractor now produces different builder function names (plan 01). The test harness source strings embed DSL calls that must match the generated function names. This plan updates those strings and confirms the full test suite passes end-to-end.

Output: Updated test harnesses, all codegen + integration tests green.
</objective>

<execution_context>
@/Users/nick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-child-table-named-builder-functions/05-CONTEXT.md
@.planning/phases/05-child-table-named-builder-functions/05-01-SUMMARY.md

<interfaces>
<!-- New builder function names after plan 01 -->

Builder function names generated by updated MetadataExtractor:
- OrganizationBuilder: has `appUser { }` method (was `organization { }`) -- because app_user.organization_id stripped = "organization" matches parent "organization" -> child table name "appUser"
- AppUserBuilder: has `createdBy { }` and `updatedBy { }` methods (unchanged) -- because task.created_by/updated_by stripped != "app_user"
- CategoryBuilder: has `category { }` method (was `childCategory { }`) -- because category.parent_id is self-ref -> table name "category"

Note: AppUserBuilder also gains inbound FK builders from task, named `createdBy` and `updatedBy` (task's outbound FKs become app_user's inbound FK child builders). The method names on AppUserBuilder for task children are `createdBy` and `updatedBy` -- these are UNCHANGED from v0.1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CodeGeneratorTest harness strings</name>
  <files>codegen/src/test/kotlin/com/nickanderssohn/declarativejooq/codegen/CodeGeneratorTest.kt</files>
  <read_first>
    - codegen/src/test/kotlin/com/nickanderssohn/declarativejooq/codegen/CodeGeneratorTest.kt
    - .planning/phases/05-child-table-named-builder-functions/05-CONTEXT.md
  </read_first>
  <action>
Update TWO harness source string methods in CodeGeneratorTest.kt:

**1. `testHarnessSource()` method (around line 146):**

Replace every occurrence of the nested `organization { }` call (which was the FK-col-named builder for app_user under organization) with `appUser { }`.

Current (lines 163-168):
```kotlin
organization {
    name = "Acme"
    organization {
        name = "Alice"
        email = "alice@acme.com"
    }
}
```

New:
```kotlin
organization {
    name = "Acme"
    appUser {
        name = "Alice"
        email = "alice@acme.com"
    }
}
```

Also update the comment on line 159 from `"Note: builder function is named after FK column (organization_id -> organization)"` to `"Note: builder function is named after child table (organization_id matches parent -> appUser)"`.

**2. `edgeCaseHarnessSource()` method (around line 248):**

In `runSelfRef` function: replace `childCategory { }` with `category { }` (4 occurrences in the nested block):
```kotlin
category {
    name = "Electronics"
    category {
        name = "Phones"
        category {
            name = "Smartphones"
        }
    }
}
```

In `runMultiFk` function: replace the nested `organization { }` calls (app_user builders) with `appUser { }`:
```kotlin
organization {
    name = "Acme"
    appUser {
        name = "Alice"
        email = "alice@acme.com"
        createdBy {
            title = "Created Task"
        }
    }
}
```

**Summary of all string replacements in CodeGeneratorTest.kt:**
- `testHarnessSource()`: `organization {` (the NESTED one for app_user, not the root) -> `appUser {` (1 occurrence)
- `edgeCaseHarnessSource()` / `runSelfRef`: `childCategory {` -> `category {` (2 occurrences)
- `edgeCaseHarnessSource()` / `runMultiFk`: nested `organization {` (for app_user) -> `appUser {` (1 occurrence)

Total: 4 string replacements (matching the CONTEXT.md count of "6 occurrences for organization -> appUser" across both files, plus "4 occurrences for childCategory -> category" across both files).

Do NOT change:
- The ROOT `organization { }` call on the DslScope (this is the DslScope extension function, not a builder function)
- The `createdBy { }` / `updatedBy { }` calls (these are unchanged)
- The `multipleFkNaming` test assertions (they already assert `createdBy`/`updatedBy` and `task !in methodNames` -- still valid)
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && ./gradlew :dsl-runtime:testClasses && ./gradlew :codegen:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CodeGeneratorTest.kt `testHarnessSource()` contains `appUser {` (not `organization {` for the nested app_user builder)
    - CodeGeneratorTest.kt `edgeCaseHarnessSource()` `runSelfRef` contains `category {` nested calls (not `childCategory {`)
    - CodeGeneratorTest.kt `edgeCaseHarnessSource()` `runMultiFk` contains `appUser {` (not `organization {` for nested app_user)
    - CodeGeneratorTest.kt does NOT contain `childCategory` anywhere
    - CodeGeneratorTest.kt root-level `organization {` calls on DslScope are preserved (still present)
    - `./gradlew :codegen:test` passes all tests (exit code 0), including generatedCodeCompiles, fkResolutionWorks, selfReferentialFkInserts, multipleFkNaming, multipleFkToSameTableInserts
  </acceptance_criteria>
  <done>All CodeGeneratorTest harness strings updated from v0.1 FK-column-named builders to v0.2 child-table-named builders. All 8 compile-and-run tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update FullPipelineTest harness strings</name>
  <files>integration-tests/src/test/kotlin/com/nickanderssohn/declarativejooq/integration/FullPipelineTest.kt</files>
  <read_first>
    - integration-tests/src/test/kotlin/com/nickanderssohn/declarativejooq/integration/FullPipelineTest.kt
    - .planning/phases/05-child-table-named-builder-functions/05-CONTEXT.md
  </read_first>
  <action>
Update the `integrationHarnessSource()` method in FullPipelineTest.kt. Apply the same two categories of replacement:

**1. Replace nested `organization { }` (app_user builder) with `appUser { }` in these functions:**

- `runBasic`: 1 occurrence (line ~122)
- `runMultipleSameType`: 2 occurrences (lines ~134, ~138)
- `runMultiLevel`: 1 occurrence (line ~152)
- `runMultiFk`: 1 occurrence (line ~183)
- `runMixedGraph`: 2 occurrences (lines ~199, ~207)

Total: 7 replacements of nested `organization {` -> `appUser {`

**2. Replace `childCategory { }` with `category { }` in these functions:**

- `runSelfRef`: 2 occurrences (lines ~168, ~170)
- `runMixedGraph`: 1 occurrence (line ~214)

Total: 3 replacements of `childCategory {` -> `category {`

**Example of correct `runBasic` after update:**
```kotlin
fun runBasic(ctx: DSLContext): DslResult {
    return execute(ctx) {
        organization {
            name = "Acme"
            appUser {
                name = "Alice"
                email = "alice@acme.com"
            }
        }
    }
}
```

**Example of correct `runSelfRef` after update:**
```kotlin
fun runSelfRef(ctx: DSLContext): DslResult {
    return execute(ctx) {
        category {
            name = "Electronics"
            category {
                name = "Phones"
                category {
                    name = "Smartphones"
                }
            }
        }
    }
}
```

Do NOT change:
- Root-level `organization { }` and `category { }` calls (DslScope extensions, not builder functions)
- `createdBy { }` calls (unchanged naming)
- Any SQL, assertion logic, or test structure
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && ./gradlew :dsl-runtime:testClasses && ./gradlew :codegen:test && ./gradlew :integration-tests:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - FullPipelineTest.kt `integrationHarnessSource()` does NOT contain `childCategory`
    - FullPipelineTest.kt `runBasic` contains `appUser {` (not nested `organization {` for app_user)
    - FullPipelineTest.kt `runMultipleSameType` contains two `appUser {` calls (not nested `organization {`)
    - FullPipelineTest.kt `runSelfRef` contains nested `category {` (not `childCategory {`)
    - FullPipelineTest.kt root-level `organization {` calls are preserved
    - `./gradlew :integration-tests:test` passes all 6 tests (exit code 0)
    - `./gradlew :codegen:test` still passes (regression check)
  </acceptance_criteria>
  <done>All FullPipelineTest harness strings updated. Full test suite passes: codegen unit tests, codegen compile-and-run tests, and Postgres integration tests all green.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/nick/Projects/declarative-jooq
# Full test suite
./gradlew :dsl-runtime:testClasses && ./gradlew :codegen:test && ./gradlew :integration-tests:test
```
All tests pass across all three modules.
</verification>

<success_criteria>
- Zero occurrences of `childCategory` in any test file
- Zero occurrences of nested `organization {` used as an app_user builder (only root-level DslScope `organization {` remains)
- All CodeGeneratorTest tests pass (8 tests)
- All FullPipelineTest tests pass (6 tests)
- All ScannerTest tests pass (8+ tests, from plan 01)
- Complete test suite green: `./gradlew test` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-child-table-named-builder-functions/05-02-SUMMARY.md`
</output>
