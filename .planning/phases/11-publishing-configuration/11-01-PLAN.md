---
phase: 11-publishing-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle/wrapper/gradle-wrapper.properties
  - dsl-runtime/build.gradle.kts
  - codegen/build.gradle.kts
  - gradle-plugin/build.gradle.kts
  - SETUP.md
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03

must_haves:
  truths:
    - "Version reads 1.0.0 in root build.gradle.kts (no SNAPSHOT suffix)"
    - "All 3 publishable modules apply vanniktech 0.35.0 and have complete POM metadata"
    - "Signing is configured with in-memory keys and a guard that skips signing when credentials are absent"
    - "SETUP.md documents the correct vanniktech property names (signingInMemoryKey, not signing.key)"
    - "Gradle wrapper is 8.13 (required by vanniktech 0.35.0)"
  artifacts:
    - path: "build.gradle.kts"
      provides: "Version 1.0.0 for all subprojects"
      contains: "version = \"1.0.0\""
    - path: "dsl-runtime/build.gradle.kts"
      provides: "Vanniktech publishing config for dsl-runtime"
      contains: "com.vanniktech.maven.publish"
    - path: "codegen/build.gradle.kts"
      provides: "Vanniktech publishing config for codegen"
      contains: "com.vanniktech.maven.publish"
    - path: "gradle-plugin/build.gradle.kts"
      provides: "Vanniktech publishing config for gradle-plugin"
      contains: "com.vanniktech.maven.publish"
    - path: "SETUP.md"
      provides: "Updated property names for vanniktech"
      contains: "signingInMemoryKey"
  key_links:
    - from: "dsl-runtime/build.gradle.kts"
      to: "gradle/wrapper/gradle-wrapper.properties"
      via: "vanniktech 0.35.0 requires Gradle 8.13"
      pattern: "gradle-8\\.13"
    - from: "*/build.gradle.kts"
      to: "~/.gradle/gradle.properties"
      via: "signingInMemoryKey property read by vanniktech"
      pattern: "signingInMemoryKey"
---

<objective>
Configure all three publishable modules (dsl-runtime, codegen, gradle-plugin) with vanniktech maven-publish plugin 0.35.0, complete POM metadata, in-memory GPG signing with a credential guard, and bump version to 1.0.0.

Purpose: Enable Maven Central publishing with correct artifact metadata, signatures, and version number.
Output: All module build files configured for publishing; Gradle wrapper upgraded to 8.13; SETUP.md updated with correct property names.
</objective>

<execution_context>
@/Users/nick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-publishing-configuration/11-RESEARCH.md

<interfaces>
<!-- Current build files that will be modified -->

From build.gradle.kts (root):
```kotlin
allprojects {
    version = "0.1.0-SNAPSHOT"
}
subprojects {
    group = "com.nickanderssohn"
}
```

From dsl-runtime/build.gradle.kts:
```kotlin
plugins {
    kotlin("jvm")
    `maven-publish`
}
publishing {
    publications {
        create<MavenPublication>("maven") {
            from(components["java"])
        }
    }
}
dependencies {
    compileOnly("org.jooq:jooq:3.19.16")
    // ... test deps
}
```

From codegen/build.gradle.kts:
```kotlin
plugins {
    kotlin("jvm")
    `maven-publish`
}
publishing {
    publications {
        create<MavenPublication>("maven") {
            from(components["java"])
        }
    }
}
dependencies {
    implementation("com.squareup:kotlinpoet:2.2.0")
    implementation("io.github.classgraph:classgraph:4.8.181")
    compileOnly("org.jooq:jooq:3.19.16")
    // ... test deps
}
```

From gradle-plugin/build.gradle.kts:
```kotlin
plugins {
    kotlin("jvm")
    `java-gradle-plugin`
    `maven-publish`
}
gradlePlugin {
    plugins {
        create("declarativeJooq") {
            id = "com.nickanderssohn.declarative-jooq"
            implementationClass = "com.nickanderssohn.declarativejooq.gradle.DeclarativeJooqPlugin"
        }
    }
}
```

From gradle/wrapper/gradle-wrapper.properties:
```
distributionUrl=https\://services.gradle.org/distributions/gradle-8.12-bin.zip
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Gradle wrapper, bump version, update SETUP.md property names</name>
  <files>build.gradle.kts, gradle/wrapper/gradle-wrapper.properties, SETUP.md</files>
  <read_first>
    - build.gradle.kts (current version string and allprojects block)
    - gradle/wrapper/gradle-wrapper.properties (current Gradle version)
    - SETUP.md (current property names in section 5)
  </read_first>
  <action>
1. **Upgrade Gradle wrapper to 8.13** (required by vanniktech 0.35.0):
   ```bash
   ./gradlew wrapper --gradle-version=8.13
   ```
   This updates `gradle/wrapper/gradle-wrapper.properties` to `gradle-8.13-bin.zip` and regenerates the wrapper JAR.

2. **Bump version in root build.gradle.kts:**
   Change `version = "0.1.0-SNAPSHOT"` to `version = "1.0.0"` in the `allprojects` block. The full block becomes:
   ```kotlin
   allprojects {
       version = "1.0.0"
   }
   ```

3. **Update SETUP.md section 5 (Local gradle.properties):**
   Replace the "Preferred: in-memory signing" properties block. The current SETUP.md uses legacy property names (`signing.keyId`, `signing.password`, `signing.key`, `sonatypeUsername`, `sonatypePassword`). Replace with vanniktech's property names:
   ```properties
   # Vanniktech in-memory signing
   signingInMemoryKeyId=SIGNING_KEY_ID_VALUE
   signingInMemoryKeyPassword=SIGNING_PASSWORD_VALUE
   signingInMemoryKey=BASE64_ENCODED_ARMORED_KEY

   # Maven Central credentials
   mavenCentralUsername=SONATYPE_USERNAME_VALUE
   mavenCentralPassword=SONATYPE_PASSWORD_VALUE
   ```

   Also update the explanatory text: change "signing.key is the same base64 output" to "signingInMemoryKey is the same base64 output as the SIGNING_KEY GitHub Secret."

   Update the "Alternative: keyring file" section to note it is NOT compatible with the vanniktech plugin configuration used by this project.

   Remove the final line "Phase 11 publishing config expects `signing.key`, not a keyring file path." and replace with "Phase 11 publishing config uses vanniktech's `signingInMemoryKey` properties. The legacy `signing.key` / `signing.keyId` / `signing.password` names are NOT recognized by the vanniktech plugin."
  </action>
  <verify>
    <automated>grep -q 'version = "1.0.0"' build.gradle.kts && grep -q 'gradle-8.13' gradle/wrapper/gradle-wrapper.properties && grep -q 'signingInMemoryKey' SETUP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts contains `version = "1.0.0"` (no SNAPSHOT)
    - gradle/wrapper/gradle-wrapper.properties contains `gradle-8.13-bin.zip`
    - SETUP.md section 5 contains `signingInMemoryKeyId=` (not `signing.keyId=`)
    - SETUP.md section 5 contains `signingInMemoryKeyPassword=` (not `signing.password=`)
    - SETUP.md section 5 contains `signingInMemoryKey=` (not `signing.key=`)
    - SETUP.md section 5 contains `mavenCentralUsername=` (not `sonatypeUsername=`)
    - SETUP.md section 5 contains `mavenCentralPassword=` (not `sonatypePassword=`)
    - build.gradle.kts does NOT contain `SNAPSHOT`
  </acceptance_criteria>
  <done>Gradle wrapper is 8.13, version is 1.0.0, SETUP.md has correct vanniktech property names</done>
</task>

<task type="auto">
  <name>Task 2: Configure vanniktech publishing on all 3 modules with POM metadata and signing guard</name>
  <files>dsl-runtime/build.gradle.kts, codegen/build.gradle.kts, gradle-plugin/build.gradle.kts</files>
  <read_first>
    - dsl-runtime/build.gradle.kts (current plugins, publishing, dependencies)
    - codegen/build.gradle.kts (current plugins, publishing, dependencies)
    - gradle-plugin/build.gradle.kts (current plugins, gradlePlugin block, dependencies)
    - .planning/phases/11-publishing-configuration/11-RESEARCH.md (vanniktech config patterns)
  </read_first>
  <action>
For each of the 3 modules, apply the vanniktech plugin and replace the existing `maven-publish` plugin + `publishing {}` block. Keep all existing `dependencies`, `tasks.test`, and (for gradle-plugin) `gradlePlugin` blocks unchanged.

**dsl-runtime/build.gradle.kts** — full target state:
```kotlin
plugins {
    kotlin("jvm")
    id("com.vanniktech.maven.publish") version "0.35.0"
}

mavenPublishing {
    publishToMavenCentral(com.vanniktech.maven.publish.SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)
    signAllPublications()

    coordinates("com.nickanderssohn", "declarative-jooq-dsl-runtime", project.version.toString())

    pom {
        name.set("declarative-jooq DSL Runtime")
        description.set("Declarative test data creation for jOOQ — handles insertion order, FK assignment, and result assembly.")
        inceptionYear.set("2024")
        url.set("https://github.com/nickanderssohn/declarative-jooq")

        licenses {
            license {
                name.set("The Apache License, Version 2.0")
                url.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
                distribution.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
            }
        }

        developers {
            developer {
                id.set("nickanderssohn")
                name.set("Nick Anderssohn")
                url.set("https://github.com/nickanderssohn/")
            }
        }

        scm {
            url.set("https://github.com/nickanderssohn/declarative-jooq/")
            connection.set("scm:git:git://github.com/nickanderssohn/declarative-jooq.git")
            developerConnection.set("scm:git:ssh://git@github.com/nickanderssohn/declarative-jooq.git")
        }
    }
}

tasks.withType<Sign>().configureEach {
    enabled = project.findProperty("signingInMemoryKey") != null
}

dependencies {
    compileOnly("org.jooq:jooq:3.19.16")
    testImplementation("org.jooq:jooq:3.19.16")
    testImplementation("com.h2database:h2:2.3.232")
    testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
}

tasks.test {
    useJUnitPlatform()
    testLogging {
        events("passed", "failed", "skipped")
    }
}
```

**codegen/build.gradle.kts** — full target state:
```kotlin
plugins {
    kotlin("jvm")
    id("com.vanniktech.maven.publish") version "0.35.0"
}

mavenPublishing {
    publishToMavenCentral(com.vanniktech.maven.publish.SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)
    signAllPublications()

    coordinates("com.nickanderssohn", "declarative-jooq-codegen", project.version.toString())

    pom {
        name.set("declarative-jooq Code Generator")
        description.set("Code generator that produces type-safe Kotlin DSL builders from jOOQ TableImpl classes for declarative test data creation.")
        inceptionYear.set("2024")
        url.set("https://github.com/nickanderssohn/declarative-jooq")

        licenses {
            license {
                name.set("The Apache License, Version 2.0")
                url.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
                distribution.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
            }
        }

        developers {
            developer {
                id.set("nickanderssohn")
                name.set("Nick Anderssohn")
                url.set("https://github.com/nickanderssohn/")
            }
        }

        scm {
            url.set("https://github.com/nickanderssohn/declarative-jooq/")
            connection.set("scm:git:git://github.com/nickanderssohn/declarative-jooq.git")
            developerConnection.set("scm:git:ssh://git@github.com/nickanderssohn/declarative-jooq.git")
        }
    }
}

tasks.withType<Sign>().configureEach {
    enabled = project.findProperty("signingInMemoryKey") != null
}

dependencies {
    implementation("com.squareup:kotlinpoet:2.2.0")
    implementation("io.github.classgraph:classgraph:4.8.181")
    compileOnly("org.jooq:jooq:3.19.16")

    testImplementation("org.jooq:jooq:3.19.16")
    testImplementation("com.h2database:h2:2.3.232")
    testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
    testImplementation("com.github.tschuchortdev:kotlin-compile-testing:1.6.0")
    testImplementation(project(":dsl-runtime"))
}

tasks.test {
    useJUnitPlatform()
    testLogging {
        events("passed", "failed", "skipped")
    }
    dependsOn(":dsl-runtime:testClasses")
}
```

**gradle-plugin/build.gradle.kts** — full target state:
```kotlin
plugins {
    kotlin("jvm")
    `java-gradle-plugin`
    id("com.vanniktech.maven.publish") version "0.35.0"
}

mavenPublishing {
    publishToMavenCentral(com.vanniktech.maven.publish.SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)
    signAllPublications()

    coordinates("com.nickanderssohn", "declarative-jooq-gradle-plugin", project.version.toString())

    pom {
        name.set("declarative-jooq Gradle Plugin")
        description.set("Gradle plugin that generates type-safe Kotlin DSL builders from jOOQ TableImpl classes for declarative test data creation.")
        inceptionYear.set("2024")
        url.set("https://github.com/nickanderssohn/declarative-jooq")

        licenses {
            license {
                name.set("The Apache License, Version 2.0")
                url.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
                distribution.set("https://www.apache.org/licenses/LICENSE-2.0.txt")
            }
        }

        developers {
            developer {
                id.set("nickanderssohn")
                name.set("Nick Anderssohn")
                url.set("https://github.com/nickanderssohn/")
            }
        }

        scm {
            url.set("https://github.com/nickanderssohn/declarative-jooq/")
            connection.set("scm:git:git://github.com/nickanderssohn/declarative-jooq.git")
            developerConnection.set("scm:git:ssh://git@github.com/nickanderssohn/declarative-jooq.git")
        }
    }
}

tasks.withType<Sign>().configureEach {
    enabled = project.findProperty("signingInMemoryKey") != null
}

gradlePlugin {
    plugins {
        create("declarativeJooq") {
            id = "com.nickanderssohn.declarative-jooq"
            implementationClass = "com.nickanderssohn.declarativejooq.gradle.DeclarativeJooqPlugin"
        }
    }
}

dependencies {
    implementation(project(":codegen"))
    implementation("org.jooq:jooq:3.19.16")
    testImplementation(gradleTestKit())
    testImplementation("org.junit.jupiter:junit-jupiter:5.11.4")
}

tasks.test {
    useJUnitPlatform()
    testLogging {
        events("passed", "failed", "skipped")
    }
}
```

**CRITICAL differences from existing files:**
- `maven-publish` plugin REMOVED from all 3 modules (vanniktech replaces it)
- `publishing { publications { ... } }` blocks REMOVED from dsl-runtime and codegen (vanniktech handles publications)
- `id("com.vanniktech.maven.publish") version "0.35.0"` ADDED to all 3 modules
- `mavenPublishing { ... }` block ADDED to all 3 modules with full POM metadata
- `tasks.withType<Sign>().configureEach { enabled = ... }` guard ADDED to all 3 modules
- `java-gradle-plugin` kept in gradle-plugin (vanniktech auto-detects it for plugin marker artifact)
- `import` for `Sign` class: use fully qualified `org.gradle.plugins.signing.Sign` if needed, but Gradle DSL typically resolves it automatically
- `coordinates()` uses `project.version.toString()` to read version from root allprojects block

**Artifact naming convention:** Use `declarative-jooq-dsl-runtime`, `declarative-jooq-codegen`, `declarative-jooq-gradle-plugin` as artifact IDs (prefixed with project name for Maven Central discoverability).
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && ./gradlew :dsl-runtime:publishToMavenLocal :codegen:publishToMavenLocal :gradle-plugin:publishToMavenLocal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - dsl-runtime/build.gradle.kts contains `id("com.vanniktech.maven.publish") version "0.35.0"`
    - dsl-runtime/build.gradle.kts contains `coordinates("com.nickanderssohn", "declarative-jooq-dsl-runtime"`
    - dsl-runtime/build.gradle.kts contains `name.set("declarative-jooq DSL Runtime")`
    - dsl-runtime/build.gradle.kts contains `licenses {`
    - dsl-runtime/build.gradle.kts contains `developers {`
    - dsl-runtime/build.gradle.kts contains `scm {`
    - dsl-runtime/build.gradle.kts contains `findProperty("signingInMemoryKey")`
    - dsl-runtime/build.gradle.kts does NOT contain `maven-publish` (bare plugin ID)
    - dsl-runtime/build.gradle.kts does NOT contain `publishing {`
    - codegen/build.gradle.kts contains `id("com.vanniktech.maven.publish") version "0.35.0"`
    - codegen/build.gradle.kts contains `coordinates("com.nickanderssohn", "declarative-jooq-codegen"`
    - codegen/build.gradle.kts does NOT contain `maven-publish`
    - codegen/build.gradle.kts does NOT contain `publishing {`
    - gradle-plugin/build.gradle.kts contains `id("com.vanniktech.maven.publish") version "0.35.0"`
    - gradle-plugin/build.gradle.kts contains `coordinates("com.nickanderssohn", "declarative-jooq-gradle-plugin"`
    - gradle-plugin/build.gradle.kts contains `java-gradle-plugin` (kept for plugin marker)
    - gradle-plugin/build.gradle.kts does NOT contain bare `maven-publish`
    - All 3 module build files contain `publishToMavenCentral` with `automaticRelease = false`
    - All 3 module build files contain `signAllPublications()`
    - `./gradlew :dsl-runtime:publishToMavenLocal :codegen:publishToMavenLocal :gradle-plugin:publishToMavenLocal` exits 0
  </acceptance_criteria>
  <done>All 3 modules configured with vanniktech 0.35.0, complete POM metadata (name, description, url, licenses, developers, scm), signing with guard, and publishing config validated via publishToMavenLocal</done>
</task>

</tasks>

<verification>
- `grep -q 'version = "1.0.0"' build.gradle.kts` confirms version bump
- `grep -q 'gradle-8.13' gradle/wrapper/gradle-wrapper.properties` confirms wrapper upgrade
- `grep -c 'com.vanniktech.maven.publish' dsl-runtime/build.gradle.kts codegen/build.gradle.kts gradle-plugin/build.gradle.kts` returns 1 for each
- `grep -c 'signingInMemoryKey' dsl-runtime/build.gradle.kts codegen/build.gradle.kts gradle-plugin/build.gradle.kts` returns 1 for each
- `./gradlew :dsl-runtime:publishToMavenLocal :codegen:publishToMavenLocal :gradle-plugin:publishToMavenLocal` passes
</verification>

<success_criteria>
All 3 publishable modules have vanniktech plugin configured with POM metadata and signing guard. Version is 1.0.0. Gradle wrapper is 8.13. SETUP.md has correct property names. Publishing config validated via publishToMavenLocal.
</success_criteria>

<output>
After completion, create `.planning/phases/11-publishing-configuration/11-01-SUMMARY.md`
</output>
