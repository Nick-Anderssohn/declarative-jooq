---
phase: 11-publishing-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - dsl-runtime/build.gradle.kts
  - codegen/build.gradle.kts
  - gradle-plugin/build.gradle.kts
autonomous: false
requirements:
  - PUB-04

must_haves:
  truths:
    - "publishToMavenLocal succeeds for all 3 modules without error"
    - "Each module produces main JAR, sources JAR, javadoc JAR, and POM in ~/.m2"
    - "POM files contain all required Maven Central fields (name, description, url, licenses, developers, scm)"
    - "Running publishToMavenLocal without signing credentials does not fail (signing guard works)"
  artifacts:
    - path: "~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/"
      provides: "Published dsl-runtime artifacts"
      contains: "declarative-jooq-dsl-runtime-1.0.0.jar"
    - path: "~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/"
      provides: "Published codegen artifacts"
      contains: "declarative-jooq-codegen-1.0.0.jar"
    - path: "~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/"
      provides: "Published gradle-plugin artifacts"
      contains: "declarative-jooq-gradle-plugin-1.0.0.jar"
  key_links:
    - from: "*/build.gradle.kts"
      to: "~/.m2/repository/com/nickanderssohn/"
      via: "publishToMavenLocal task"
      pattern: "publishToMavenLocal"
---

<objective>
Verify that `./gradlew publishToMavenLocal` succeeds for all 3 modules and produces the complete artifact set required by Maven Central, then have the user confirm artifact correctness.

Purpose: Validate that the publishing configuration from Plan 01 actually works end-to-end.
Output: Verified local Maven repository with all required artifacts.
</objective>

<execution_context>
@/Users/nick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-publishing-configuration/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run publishToMavenLocal and verify all artifacts</name>
  <files>dsl-runtime/build.gradle.kts, codegen/build.gradle.kts, gradle-plugin/build.gradle.kts</files>
  <read_first>
    - dsl-runtime/build.gradle.kts (confirm vanniktech config is in place)
    - codegen/build.gradle.kts (confirm vanniktech config is in place)
    - gradle-plugin/build.gradle.kts (confirm vanniktech config is in place)
  </read_first>
  <action>
1. **Clean any previous local Maven artifacts** to ensure fresh verification:
   ```bash
   rm -rf ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0
   rm -rf ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0
   rm -rf ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0
   ```

2. **Run publishToMavenLocal for all modules:**
   ```bash
   ./gradlew publishToMavenLocal
   ```
   This must exit 0. If it fails, diagnose and fix the issue in the module build files (dsl-runtime/build.gradle.kts, codegen/build.gradle.kts, gradle-plugin/build.gradle.kts). These files are listed in this task's `<files>` to cover the self-repair contingency.

3. **Verify artifact presence for dsl-runtime:**
   ```bash
   ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/
   ```
   Must contain at minimum: `declarative-jooq-dsl-runtime-1.0.0.jar`, `declarative-jooq-dsl-runtime-1.0.0-sources.jar`, `declarative-jooq-dsl-runtime-1.0.0-javadoc.jar`, `declarative-jooq-dsl-runtime-1.0.0.pom`

4. **Verify artifact presence for codegen:**
   ```bash
   ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/
   ```
   Must contain: `declarative-jooq-codegen-1.0.0.jar`, `-sources.jar`, `-javadoc.jar`, `.pom`

5. **Verify artifact presence for gradle-plugin:**
   ```bash
   ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/
   ```
   Must contain: `declarative-jooq-gradle-plugin-1.0.0.jar`, `-sources.jar`, `-javadoc.jar`, `.pom`

6. **Verify POM content for dsl-runtime (spot check):**
   ```bash
   cat ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/declarative-jooq-dsl-runtime-1.0.0.pom
   ```
   Verify the POM XML contains: `<name>`, `<description>`, `<url>`, `<licenses>`, `<developers>`, `<scm>` elements.

7. **Verify version in POM:**
   ```bash
   grep '<version>1.0.0</version>' ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/declarative-jooq-dsl-runtime-1.0.0.pom
   ```

8. **Verify plugin marker artifact exists** (for gradle-plugin only):
   ```bash
   ls ~/.m2/repository/com/nickanderssohn/declarative-jooq/com.nickanderssohn.declarative-jooq.gradle.plugin/1.0.0/
   ```
   Must contain a `.pom` file (this is the plugin marker that lets `plugins { id("com.nickanderssohn.declarative-jooq") }` resolve).

9. **Test signing guard** — verify publishToMavenLocal works without signing credentials:
   If the machine has `signingInMemoryKey` in `~/.gradle/gradle.properties`, temporarily verify the guard logic is correct by confirming the `tasks.withType<Sign>().configureEach` block exists in each build file. The actual unsigned publish test is: if no `signingInMemoryKey` property is set, signing tasks should be skipped and publish should still succeed.

If any verification step fails, fix the issue in the relevant module's `build.gradle.kts` and re-run.
  </action>
  <verify>
    <automated>cd /Users/nick/Projects/declarative-jooq && ./gradlew publishToMavenLocal 2>&1 | tail -5 && echo "---" && ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/*.jar ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/*.pom 2>/dev/null && echo "---" && ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/*.jar ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/*.pom 2>/dev/null && echo "---" && ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/*.jar ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/*.pom 2>/dev/null && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew publishToMavenLocal` exits 0
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/ contains declarative-jooq-dsl-runtime-1.0.0.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/ contains declarative-jooq-dsl-runtime-1.0.0-sources.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/ contains declarative-jooq-dsl-runtime-1.0.0-javadoc.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/ contains declarative-jooq-dsl-runtime-1.0.0.pom
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/ contains declarative-jooq-codegen-1.0.0.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/ contains declarative-jooq-codegen-1.0.0-sources.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/ contains declarative-jooq-codegen-1.0.0-javadoc.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/ contains declarative-jooq-codegen-1.0.0.pom
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/ contains declarative-jooq-gradle-plugin-1.0.0.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/ contains declarative-jooq-gradle-plugin-1.0.0-sources.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/ contains declarative-jooq-gradle-plugin-1.0.0-javadoc.jar
    - ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/ contains declarative-jooq-gradle-plugin-1.0.0.pom
    - dsl-runtime POM contains `<name>declarative-jooq DSL Runtime</name>`
    - dsl-runtime POM contains `<licenses>`
    - dsl-runtime POM contains `<developers>`
    - dsl-runtime POM contains `<scm>`
    - Plugin marker POM exists at ~/.m2/repository/com/nickanderssohn/declarative-jooq/com.nickanderssohn.declarative-jooq.gradle.plugin/1.0.0/
  </acceptance_criteria>
  <done>publishToMavenLocal succeeds, all 3 modules produce main JAR + sources JAR + javadoc JAR + POM, POM has all required fields, plugin marker artifact exists</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies published artifacts</name>
  <files></files>
  <action>
Present the user with the verification steps below. This is a human checkpoint — no code changes needed. The user inspects the artifacts produced by Task 1 and confirms they look correct.
  </action>
  <what-built>Complete Maven Central publishing configuration for all 3 modules (dsl-runtime, codegen, gradle-plugin) with vanniktech 0.35.0, POM metadata, signing guard, and version 1.0.0. All artifacts published to local Maven repository.</what-built>
  <how-to-verify>
    1. Check dsl-runtime artifacts: `ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/`
    2. Check codegen artifacts: `ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-codegen/1.0.0/`
    3. Check gradle-plugin artifacts: `ls ~/.m2/repository/com/nickanderssohn/declarative-jooq-gradle-plugin/1.0.0/`
    4. Spot-check a POM: `cat ~/.m2/repository/com/nickanderssohn/declarative-jooq-dsl-runtime/1.0.0/declarative-jooq-dsl-runtime-1.0.0.pom`
    5. Verify plugin marker: `ls ~/.m2/repository/com/nickanderssohn/declarative-jooq/com.nickanderssohn.declarative-jooq.gradle.plugin/1.0.0/`
    6. If you have signing credentials in ~/.gradle/gradle.properties, check for .asc files alongside the JARs
    7. Verify the SETUP.md property names match what you have in your ~/.gradle/gradle.properties
  </how-to-verify>
  <verify>User confirms artifacts are correct</verify>
  <done>User has approved the published artifacts and POM metadata</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `./gradlew publishToMavenLocal` exits 0
- All 3 modules produce: main JAR, sources JAR, javadoc JAR, POM
- POM contains name, description, url, licenses, developers, scm
- Plugin marker artifact exists for gradle-plugin
- No signing errors when credentials are absent (guard works)
</verification>

<success_criteria>
publishToMavenLocal succeeds for all 3 modules. Local Maven repository contains complete artifact sets. User confirms artifacts look correct.
</success_criteria>

<output>
After completion, create `.planning/phases/11-publishing-configuration/11-02-SUMMARY.md`
</output>
